# Frontend React

Frontend del sistema de reparaciones.

## Ejecutar

```bash
npm install
npm start
```

Por configuración del `package.json`, se abre en el puerto 3001 para no chocar con el backend.

```txt
http://localhost:3001
```

El backend debe estar activo en:

```txt
http://localhost:3000
```
